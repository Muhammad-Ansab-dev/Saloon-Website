// Route: /contact — server component shell for the ContactPage composition
// (booking card backed by the shared BookingForm, studio locations, map).
import { ContactPage } from '@/sections/contact/ContactPage';

export default function Page() {
  return <ContactPage />;
}